apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Dragonfly Operator — manages Dragonfly instances via the Dragonfly CRD.
# Installs into dragonfly-operator-system (namespace defined in the upstream manifest).
# Pin to a release tag; bump here to upgrade.
resources:
  - https://raw.githubusercontent.com/dragonflydb/dragonfly-operator/v1.5.0/manifests/dragonfly-operator.yaml
