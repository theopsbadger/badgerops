configs:
  params:
    server.insecure: true
  cm:
    kustomize.buildOptions: --enable-helm
repoServer:
  livenessProbe:
    initialDelaySeconds: 30
  readinessProbe:
    initialDelaySeconds: 30
server: {}
